# Docker Compose

> TODO: adicionar anotações sobre Docker Compose.

Sugestão de tópicos:
- Diferença entre `docker run` e `docker compose`
- Estrutura básica de um `docker-compose.yml`
- Subir múltiplos serviços (ex: app + banco de dados)
- Comandos úteis (`up`, `down`, `logs`, `ps`)

